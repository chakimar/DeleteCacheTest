<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    >
    <WebView
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:id="@+id/webview"
    android:layout_weight="0.7"></WebView>
    <Button
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:text="Show Cache Size"
    android:id="@+id/btn_show_cache_size"
    android:layout_weight="0.1"></Button>
    <Button
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:text="Create Cache"
    android:id="@+id/btn_create_cache"
    android:layout_weight="0.1"></Button>
    <Button
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:text="Delete Cache"
    android:id="@+id/btn_delete_cache"
    android:layout_weight="0.1"></Button>
</LinearLayout>
